/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ListRecipes from './src/screens/ListRecipes';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ListRecipes);
